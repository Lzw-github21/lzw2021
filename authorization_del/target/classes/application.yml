spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/user?useSSL=false&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&autoReconnect=true

    # ����ǰ��Thymeleafģ������
  main:
    allow-bean-definition-overriding: true
    # ���ĩβ��/
    prefix: classpath:/templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    servlet:
      content-type: text/html
    mode: HTML5
    # ��ֹ��ʵ��ǰ���Ȳ���
    cache: false

# ����Mybatis
mybatis:
  # Mybatisӳ��
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.hly.springBootSecurityMybatis.entity

# �˿�����
server:
  port: 8081

